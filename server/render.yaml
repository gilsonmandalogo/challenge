services:
  - type: web
    name: degen-coin-flip-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: CORS_ORIGIN
      - key: HOUSE_EDGE
        value: 0.035
